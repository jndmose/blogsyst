package blog.controllers;

import org.springframework.stereotype.Controller;

@Controller
public class LoginController {

	/*
	 * @Autowired private UserService userService;
	 * 
	 * @Autowired private NotificationService notifyService;
	 * 
	 * @RequestMapping("/users/login") public String login(LoginForm loginForm)
	 * { return "users/login"; }
	 * 
	 * @RequestMapping(value = "/users/login", method = RequestMethod.POST)
	 * public String loginPage(@Valid LoginForm loginForm, BindingResult
	 * bindingResult) { if (bindingResult.hasErrors()) {
	 * notifyService.addErrorMessage("Please fill the form correctly!"); return
	 * "users/login"; } if (!userService.authenticate(
	 * 
	 * loginForm.getUsername(), loginForm.getPassword())) {
	 * notifyService.addErrorMessage("Invalid login!"); return "users/login"; }
	 * notifyService.addInfoMessage("Login successful"); return "redirect:/"; }
	 */
}
